# プロジェクト概要

## 目的

Docker コンテナ上で Google Gemma4 モデルを簡単に起動・利用できる環境を提供する。

## 背景

- LLM のローカル実行ニーズが高まっている
- Gemma4 はオープンウェイトモデルとして高い性能を持つ
- Docker による環境構築で再現性とポータビリティを確保する

## スコープ

- Gemma4 モデルの Docker コンテナ化
- 推論 API の提供
- GPU / CPU 両環境での動作サポート
