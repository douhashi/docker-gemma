# コーディングルール

## Dockerfile

- マルチステージビルドを活用し、イメージサイズを最小化する
- ベースイメージのタグは明示的に指定する（`latest` を避ける）
- 不要なレイヤーを減らすため、`RUN` 命令はできるだけまとめる

## 設定ファイル

- 環境変数で設定を外部化する
- シークレット情報はイメージに含めない

## ドキュメント

- 変更に伴うドキュメント更新を忘れない
- Markdown 形式で記述する
